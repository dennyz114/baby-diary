import React from 'react'

import './BlinkingDot.scss'

const BlinkingDot = () => {
  return (
    <div className={'blinking-dot-container'}>
      <div className={'blinking-dot'} />
    </div>
  )
}

export default BlinkingDot
